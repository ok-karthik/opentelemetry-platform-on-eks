**/.terraform**
**/.terraform.**
**/.terragrunt

# Terraform state and lock files
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
.terraform.tfstate.lock.info
terraform.tfstate**


# Terraform variables
*.tfvars
*.tfvars.json

# Kubernetes Secrets
*secret.yaml
*secrets.yaml

karpenter-v*.tgz
.DS_Store

# Compiled Go binaries (`go build` in apps-src drops them next to the source)
apps-workload-cluster-1/apps-src/golang-app/golang-product-service

# Python venv and Makefile scratch output
.venv/
.tmp-manifests/
.helm-render/